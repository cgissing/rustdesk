name: Flutter Nightly Build

on:
  # Keep upstream's full platform nightly matrix manual-only in this fork.
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
